#include <stdio.h>
// Area of a Rectangle

int main()
{
    // int length;
    // int width;
    int length, width;
    printf("Enter the length\t ");
    scanf("%d", &length);
    printf("Enter the width\t ");
    scanf("%d", &width);
    printf("The area of the rectangle is %d", length * width);
    return 0;
}
